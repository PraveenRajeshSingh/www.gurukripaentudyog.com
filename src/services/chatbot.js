/**
 * Gurukripa Bricks - AI Chatbot Service
 * Lightweight — does NOT create DOM elements. Works with existing HTML chatbot.
 * The actual chatbot toggle & messaging is handled by App.initChatbot() in main.js
 */

// This file is kept for backwards compatibility but the chatbot
// functionality is now integrated into main.js App.initChatbot().
// No duplicate DOM creation happens.
console.log('💬 Chatbot module loaded.');
